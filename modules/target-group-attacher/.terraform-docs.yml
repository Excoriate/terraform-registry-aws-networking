---
formatter: markdown table

header-from: main.tf
content: |-
    # ☁️ Application Load Balancer Target Group attacher
    ## Description
    {{ .Header }}
    This module creates an Application Load Balancer Target Group attacher.
    * 🚀 It creates one or many target group attachments
    * 🚀 It defines the port where the traffic can be redirected.

    ---
    ## Example
    Examples of this module's usage are available in the [examples](./examples) folder.

    ```hcl
    {{ include "../../examples/target-group-attacher/basic/main.tf" }}
    ```
    ---

    ## Module's documentation
    (This documentation is auto-generated using [terraform-docs](https://terraform-docs.io))
    {{ .Providers }}

    {{ .Modules }}

    {{ .Resources }}

    {{ .Requirements }}

    {{ .Inputs }}

    {{ .Outputs }}

output:
    file: README.md
    mode: inject
    template: |-
        <!-- BEGIN_TF_DOCS -->
        {{ .Content }}
        <!-- END_TF_DOCS -->
settings:
    anchor: true
    color: true
